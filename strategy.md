# 期貨無限換倉＋網格自動化交易系統 — 需求規格書 (SRS)

## 專案概述
本專案旨在建構一個部署於封閉式 / 離線 Linux (Ubuntu) 環境的自動化量化交易系統。策略核心結合了**不對稱網格交易**與**期貨動態保證金管理**，並具備**自動換倉（Rollover）機制**與**選擇權合成避險功能**。系統設計要求高度模組化，以便在回測、模擬實盤與未來真實實盤之間無縫切換，並具備本地資料庫持久化與獨立的監控儀表板。

---

## 一、 策略詳細描述 (Strategy Specification)

本策略本質為「改良型反向正二策略」（下跌加倉、上漲減倉），旨在透過超額保證金降低期貨槓桿，並利用網格與換倉優化在震盪慢牛行情中的收益。

### 1. 網格建倉與平倉邏輯

* **不對稱網格 (Asymmetric Grid)：**
  * **基準步長：** 下跌 1% 執行**加倉（買入多單）**；上漲 2% 執行**減倉（賣出平倉）**。
  * **動態間距（防破網）：** 依持倉口數分級擴張加倉步長，避免單邊連續下跌造成爆倉：

    | 持倉區間 | 加倉步長 | 說明 |
    |---|---|---|
    | 0–4 口 | 下跌 1% | 基準網格 |
    | 5–6 口 | 下跌 2% | 一倍擴張 |
    | 7–8 口 | 下跌 4% | 二倍擴張 |
    | 8 口以上 | 暫停加倉 | 保留減倉邏輯，等待反彈或選擇權避險介入 |

    > 口數上限與各級閥值均為可設定參數（見 §六 config.yaml），不寫死於程式碼中。

* **最低持倉初始化：**
  * 系統最低持倉底限預設為 **2 口**。
  * 系統首次啟動時，若帳戶無任何持倉，以**市價單**買入 2 口建立底倉，再開始鋪設網格限價單。
  * 若啟動時已有持倉，則跳過初始化，直接根據現有持倉口數計算並恢復網格。
  * 當大盤持續暴漲時，減倉至 2 口即停止賣出，確保不空手錯失多頭波段。

* **盤中回跌攔截：**
  * 當盤中價格自當日/波段高點回跌達 2% 時，觸發主動加倉機制。
  * **互斥規則（防雙重觸發）：** 盤中回跌攔截觸發後，設定 **30 分鐘 Cooldown**，期間盤中回跌偵測邏輯暫停，常規網格限價單仍正常運作。Cooldown 結束後才重新計算盤中最高點。此規則防止常規網格與盤中攔截在同一下跌行情中同時觸發，造成倉位意外翻倍。

### 2. 期貨無限換倉機制 (Infinite Rollover)

* **監控指標：** 系統需每日（盤後）檢查當前持倉合約的到期剩餘交易日數（Days to Expiration, DTE）。

* **換倉觸發條件：** DTE ≤ 5 個交易日（可設定，見 §六 config.yaml）。

  > **正價差磨損說明：** 台指期貨因高配息，通常以對現貨「貼水」方式報價，而非升水。「正價差磨損」係指某些特殊市場情緒下，期貨相對現貨出現升水，該升水隨到期日趨近而消失，對多單造成額外淨值損耗。本系統以固定 DTE 觸發換倉應對此風險，並記錄每次換倉的實際點差成本做為後續分析依據，不依賴複雜的即時正逆價差判斷，降低邏輯複雜度。

* **換倉執行流程：**
  1. 暫停網格交易邏輯，並撤銷（Cancel）當前合約的所有未成交限價單。
  2. 讀取當前合約的淨多單口數 N。
  3. 同時執行：**以市價平倉舊合約全部持倉，並於下一結算月之活躍新合約建立同等口數多單**。
  4. 換倉完成後，將本次換倉資訊（時間、舊/新合約代碼、成交價差、口數）寫入 `rollover_log` 資料表，作為歷史成本統計依據。
  5. 抓取新合約之最新報價，以此為基準點**重新計算並鋪設**新合約的網格限價單，恢復正常交易。

* **換倉失敗應急處理：**
  * 若舊合約平倉成功，但新合約建倉失敗（如 `OrderRejectedError`），系統必須立即：
    1. 觸發 **CRITICAL 等級警報**（通知使用者）。
    2. 暫停所有網格邏輯，進入「等待人工確認」狀態。
    3. 禁止自動重試建倉，直到使用者透過儀表板手動確認或強制恢復。

### 3. 資金控管與動態風險防範 (Risk & Margin Management)

* **超額保證金控管（以微台期為主）：**
  * 建議入金：**150–180 萬元**（最大持倉 8 口微台，市值約 840 萬）。
  * 以 2 口底倉起始，初始真實槓桿 ≈ 1.4x，隨加倉逐步升高。
  * 所有入金金額與上限均為可設定參數（見 §六 config.yaml）。

* **動態槓桿監控：** 系統需即時計算：
  > **真實槓桿 = 當前持倉合約總市值 ÷ 帳戶總權益（含浮動損益）**

* **風險閥值（三級制）：**

  | 槓桿水位 | 等級 | 系統動作 |
  |---|---|---|
  | > 5x | 黃色警示 | 暫停加倉，發送 WARNING 通知 |
  | > 7x | 橘色警告 | 自動市價減倉 25%，發送 WARNING 通知 |
  | > 9x | 紅色緊急 | 最大風控減倉（降至最低持倉 2 口），發送 CRITICAL 通知 |

* **耐震度數學驗算：**
  * 目標：8 口滿倉時，帳戶可承受 15% 單邊下跌不被強制平倉。
  * 所需最低淨值 = 8 口 × 105 萬/口 × 15% + 8 口 × 3.6 萬（維持保證金）= **155 萬**。
  * 因此建議入金下限設為 **150 萬**，以確保滿倉時的耐震裕度。
  * 若預算較低，可將最大口數上限下調（如改為 4 口），建議入金相應降至 **75–90 萬**。

### 4. 選擇權合成部位避險 (Options Hedging Layer)

* **觸發條件（以下三項同時成立）：**
  1. 持倉口數 ≥ 最大口數上限（預設 8 口）
  2. 且當前持倉均價虧損幅度 ≥ 5%
  3. 且距上一次賣出減倉（網格觸發）已超過 3 個交易日

* **合成策略：** 自動利用選擇權組建防禦部位（Collar 結構）：
  * **Sell Call (SC)：** 選擇 Delta ≈ −0.25 ~ −0.30 的價外 Call 履約價，收取時間價值作為持倉成本補貼。
  * **Buy Put (BP)：** 選擇成本 ≤ SC 權利金收入 30% 以內的深度價外 Put，作為極端下跌保險。
  * **示意範例：** 市價 21,000 時，SC@21,500 + BP@20,000。

* **到期日選擇：** 優先選擇剩餘 14–21 個日曆日到期之合約，兼顧時間價值與彈性。

* **重要說明：** 此避險層僅在網格暫停期間作為補充性收益保護，不取代網格主策略。網格一旦恢復減倉條件（持倉口數下降），應評估是否提前平倉選擇權部位。

---

## 二、 回測系統需求 (Backtesting Engine)

* **資料輸入：** 支援歷史 K 線（1分鐘線、日線）以及 Tick 等級的歷史資料輸入，確保可在完全離線環境中執行。所有回測參數（網格步長、口數閥值等）統一從 `config.yaml` 讀取，便於參數掃描。

* **前視偏差防範（Look-Ahead Bias）：**
  * 所有交易訊號**僅能使用當下已完成的 Tick 或已封閉的 K 棒**，不得讀取任何尚未發生的未來資料。
  * 系統須以時序（Timestamp）嚴格排序處理資料，確保每一決策點僅能訪問該時刻之前的歷史資訊。

* **資料清洗規範：**
  * 缺漏 Tick（相鄰兩筆 Tick 時間差 > 5 分鐘，非正常休市時段）：以前一筆 Tick 價格填充，並於日誌記錄警告。
  * 節日、例假日、休市日：依交易所官方行事曆跳過，不產生任何模擬訂單。
  * 異常成交量（單筆 > 均量 10 倍）：記錄警告，但不過濾（保留回測真實性）。

* **撮合模擬：**
  * 需精確模擬限價單在網格點位的觸發（Tick 穿越觸發，非 K 棒觸及）。
  * 必須將**交易手續費、期交稅以及滑價（Slippage）**納入成本計算。

* **換倉模擬：**
  * 歷史近遠月換倉點差資料需事先整理為 CSV，格式：`date, contract_sold, contract_bought, spread_points`。
  * 回測遇到 DTE ≤ 5 日時，自動從點差 CSV 查詢當日換倉成本，計入淨值。

* **Walk-Forward 驗證：**
  * 以 **6 個月**為訓練視窗、**2 個月**為樣本外測試視窗，採滾動方式驗證策略穩健性，防止過度擬合（Overfitting）。

* **輸出指標：** 需產出詳細回測報告，包含：

  | 指標 | 說明 |
  |---|---|
  | 總報酬率 / 年化報酬率 | 含換倉成本、手續費後淨值 |
  | 夏普比率（Sharpe Ratio） | 年化超額報酬 / 年化波動率 |
  | 最大回撤（MDD） | 峰值至谷值最大淨值下降幅度 |
  | 保證金使用率曲線 | 逐日真實槓桿走勢圖 |
  | 換倉成本年化磨損（%） | 歷次換倉點差累計成本折算年化 |
  | 各參數組合 Sharpe 熱力圖 | 網格步長 × 口數上限的敏感度掃描 |
  | 總交易次數 / 勝率 | 已平倉交易統計 |

---

## 三、 實盤模擬系統需求 (Paper Trading Engine)

* **數據接收：** 支援接收即時（Real-time）或模擬生成的延遲市場 Tick/K線 數據流。
* **虛擬帳戶管理：** * 在本地資料庫中建立並維護一個獨立的虛擬帳戶。
  * 模擬券商的動態保證金扣款、維持保證金檢核、部位未實現盈虧與已實現盈虧的動態更新。
* **模擬撮合邏輯：** 當市場價格觸及網格設定的限價委託價時，於本地資料庫中標記訂單為「已成交」，並動態更新持倉狀態。
* **異常容錯機制：** 必須具備模擬斷線重連後，自動比對並修復本地狀態與模擬市場狀態的邏輯。

---

## 四、 模組化 API 抽象層 (API Abstraction Layer)

為了保持系統的靈活性與未來擴充性，API 需採用**抽象基底類別（Abstract Base Class）**設計：

```python
from abc import ABC, abstractmethod

class AbstractBroker(ABC):
    # --- 連線管理 ---
    @abstractmethod
    def login(self): pass

    # --- 市場資料（事件驅動，非輪詢）---
    @abstractmethod
    def subscribe_market_data(self, symbol: str, callback) -> None: pass

    @abstractmethod
    def unsubscribe_market_data(self, symbol: str) -> None: pass

    # --- 帳戶與合約資訊 ---
    @abstractmethod
    def get_account_info(self) -> dict: pass

    @abstractmethod
    def get_margin_status(self) -> dict: pass

    @abstractmethod
    def get_contract_spec(self, symbol: str) -> dict: pass

    # --- 持倉管理 ---
    @abstractmethod
    def get_positions(self) -> list: pass

    # --- 訂單管理 ---
    @abstractmethod
    def place_order(self, order_type, price, quantity, direction) -> str: pass

    @abstractmethod
    def cancel_order(self, order_id: str) -> bool: pass

    @abstractmethod
    def get_order_status(self, order_id: str) -> dict: pass
```

**統一異常（Exception）階層：**

```python
class BrokerError(Exception): pass
class BrokerConnectionError(BrokerError): pass
class OrderRejectedError(BrokerError): pass
class InsufficientMarginError(BrokerError): pass
class ContractExpiredError(BrokerError): pass
```

**架構要求：**
* 核心交易邏輯**僅能**呼叫 `AbstractBroker` 定義的方法，不得直接呼叫任何券商 SDK。
* 採用**事件驅動架構**：策略核心透過 `subscribe_market_data(symbol, callback)` 訂閱市場資料，每筆新 Tick 到達時由回調函式觸發策略邏輯，取代主動輪詢（Polling）模式，降低延遲並減少 CPU 浪費。
* 待後續決定具體券商 API 時，只需撰寫新的子類別並切換驅動，不需修改任何核心策略代碼。
* `get_contract_spec()` 回傳合約乘數、最小跳動單位、保證金比率等規格，讓策略邏輯在換倉（台指 ↔ 微台）時自動適配，無需手動修改計算公式。

## 五、 儀表板系統需求 (Dashboard Web UI)
系統採用前後端分離架構。交易核心作為本地後台常駐服務（透過 systemd 管理）運行，所有狀態實時寫入本地資料庫。儀表板前端負責讀取資料庫進行視覺化呈現。

### 1. 後端與資料庫架構
* **技術選型：** Python 交易核心 + 本地 SQLite 資料庫（確保離線依賴性最低並維持系統輕量）。

* **SQLite 必要設定（所有連線啟動時執行）：**
  ```sql
  PRAGMA journal_mode=WAL;      -- 允許讀寫並發，防止儀表板與交易核心互相鎖定
  PRAGMA busy_timeout=5000;     -- 等待鎖釋放最多 5 秒，避免即時錯誤
  ```

* **資料表設計：**

  | 資料表 | 內容 |
  |---|---|
  | `margin_status` | 更新時間、今日餘額、原始保證金、維持保證金、可用保證金、浮動損益、風險指標、真實槓桿 |
  | `position_details` | 持倉合約、數量、成本價、最新價、未實現損益 |
  | `open_orders` | 時間、訂單號、合約、價格、數量、買賣方向 |
  | `next_trade_targets` | 下一個預期觸發的買進與賣出目標價位與合約 |
  | `rollover_log` | 換倉時間、舊/新合約代碼、換倉點差（點數）、口數（換倉成本統計用） |
  | `pending_orders` | 送出前暫存訂單（冪等機制，防崩潰重複下單）：訂單號、狀態、送出時間 |
  | `snapshots` | 每 60 秒帳戶狀態快照，崩潰後用於狀態恢復 |

### 2. 前端監控介面 (UI Layout)

**自動刷新機制：** 採用 **WebSocket 推送**，交易核心每筆新 Tick 處理後主動推送狀態更新，儀表板即時渲染，無需定時輪詢。

需具備單一頁面實時監控儀表板，包含以下區塊：

* **區塊 A - 資金與保證金狀態：** 顯眼展示可用保證金、風險指標（槓桿等級色碼：綠/黃/橘/紅）、浮動損益與當前真實槓桿率。

* **區塊 B - 主標的走勢與網格分佈圖：** 以 K 線圖形式呈現主標的走勢，並在圖表上以視覺化線條標註目前「已成交買入點」與「上方待成交賣出點」。

* **區塊 C - 下次交易目標與未成交委託單：** 條列出目前最接近市價的多空兩個網格掛單，以及當前所有掛在市場上的限價單列表。

* **區塊 D - 目前持倉明細：** 顯示當前合約代碼、持倉數量、平均成本、最新價與目前盈虧。

* **區塊 E - 歷史淨值曲線：** 以折線圖顯示帳戶總權益隨時間的走勢（從 `margin_status` 歷史記錄繪製），供趨勢判讀與績效追蹤。

* **區塊 F - 系統日誌：** 顯示最近 50 筆 INFO / WARNING / ERROR 等級日誌，方便問題排查而無需 SSH 登入伺服器。

* **控制面板（Control Panel）：**
  * **緊急停止（Emergency Stop）：** 立即暫停所有網格邏輯、撤銷所有未成交限價單。
  * **手動換倉觸發：** 強制執行換倉流程（不受 DTE 限制）。
  * **確認恢復：** 在換倉失敗或 CRITICAL 警報後，手動確認並恢復交易。

---

## 六、 系統設定規格 (Configuration)

所有策略參數均集中於 `config.yaml`，不寫死於程式碼中。策略邏輯、回測引擎、儀表板均從同一份設定檔讀取，切換標的或調整參數只需修改此檔案。

```yaml
# 合約規格（切換台指/微台只需改此區塊）
contract:
  symbol: "MXTF"              # 微台期代碼（台指期改為 "TXFF"）
  multiplier: 50               # 每點金額（台指期為 200）
  original_margin: 47000       # 原始保證金（元）
  maintenance_margin: 36000    # 維持保證金（元）

# 網格策略參數
grid:
  min_position: 2              # 最低持倉口數（底倉）
  max_position: 8              # 最大持倉口數上限
  tiers:                       # 各級別：[口數上限, 加倉步長%]
    - [4, 1.0]                 # 0–4 口：跌 1% 加倉
    - [6, 2.0]                 # 5–6 口：跌 2% 加倉
    - [8, 4.0]                 # 7–8 口：跌 4% 加倉
  sell_step_pct: 2.0           # 漲 N% 執行減倉
  intraday_drop_pct: 2.0       # 盤中自高點回跌 N% 觸發攔截
  intraday_cooldown_min: 30    # 盤中回跌攔截後 Cooldown 時間（分鐘）

# 換倉參數
rollover:
  dte_threshold: 5             # 剩餘交易日 ≤ N 時觸發換倉

# 風控參數
risk:
  leverage_warn: 5.0           # 黃色警示：暫停加倉
  leverage_reduce: 7.0         # 橘色警告：自動減倉 25%
  leverage_critical: 9.0       # 紅色緊急：最大風控減倉

# 選擇權避險觸發條件
options_hedge:
  trigger_loss_pct: 5.0        # 均價虧損達 N% 才考慮觸發
  days_since_last_sell: 3      # 距上次減倉至少 N 個交易日

# 帳戶設定
account:
  initial_capital: 1500000     # 初始入金（元）

# 告警通知（擇一設定）
notification:
  provider: "telegram"         # "telegram" 或 "line"
  token: ""                    # Telegram Bot Token 或 LINE Notify Token
  chat_id: ""                  # Telegram Chat ID（LINE 不需要）
```

---

## 七、 崩潰恢復機制 (Crash Recovery)

交易核心進程崩潰或重啟時，系統必須能安全、正確地恢復狀態，不得重複下單或遺漏持倉。

* **訂單冪等機制（防重複下單）：**
  * 所有訂單在呼叫 `place_order()` 之前，先寫入 `pending_orders` 表並標記 `status=PENDING`。
  * 收到券商確認回調後，更新為 `status=CONFIRMED`。
  * 程序啟動時，掃描 `pending_orders` 中所有 `PENDING` 記錄，逐一透過 `get_order_status()` 查詢實際狀態後補全，再繼續執行，禁止對同一筆訂單重複送出。

* **狀態核對（啟動時執行）：**
  1. 讀取本地 `position_details` 表的持倉記錄。
  2. 呼叫 `get_positions()` 取得券商端實際持倉。
  3. 若兩者數量不一致，立即暫停交易並發送 CRITICAL 警報，等待人工核對。

* **快照機制：**
  * 交易核心每 **60 秒**將帳戶狀態（淨值、持倉、槓桿）寫入 `snapshots` 表。
  * 崩潰重啟後，從最新快照讀取上次已知狀態，作為狀態核對的本地參考基準。

---

## 八、 告警通知系統 (Alert & Notification)

| 等級 | 觸發條件 | 通知內容 |
|---|---|---|
| INFO | 換倉完成、網格減倉成功、程序啟動/正常關閉 | 事件摘要 + 當前持倉與槓桿 |
| WARNING | 真實槓桿超過 5x、DTE ≤ 5 日進入換倉準備 | 當前槓桿值 + 建議操作 |
| CRITICAL | 槓桿超 9x、建倉失敗、持倉不一致、程序異常崩潰 | 詳細錯誤訊息 + 系統已自動暫停交易 |

* 通知提供商（Telegram Bot / LINE Notify）透過 `config.yaml` 設定，核心程式碼無任何 hardcode。
* CRITICAL 等級須在 **30 秒內**送出通知，不得因任何非致命例外而延遲。

---

## 九、 部署架構 (Deployment)

* **程序管理：** 交易核心以 **systemd service** 方式管理：
  * 開機後自動啟動
  * 非零退出碼時自動重啟（`Restart=on-failure`）
  * 重啟冷卻間隔 30 秒（防止崩潰循環）

* **日誌管理：**
  * 使用 Python `logging` 模組，格式：`[時間] [等級] [模組] 訊息`
  * 搭配 **logrotate**：每日輪轉、保留 30 天、超過 50 MB 強制輪轉

* **SQLite 設定：** 所有連線（交易核心與儀表板）啟動時執行：
  ```sql
  PRAGMA journal_mode=WAL;
  PRAGMA busy_timeout=5000;
  ```

* **資料備份：** 建議每日盤後以 `sqlite3 trading.db .dump > backup_YYYYMMDD.sql` 備份資料庫，保留 7 天。
